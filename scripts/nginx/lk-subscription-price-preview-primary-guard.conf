# Reuse the existing primary map: reserve enforces each client at ingress.
# Keep its trusted-proxy exemption and isolate preview from the catalog quota.
limit_req_zone $lk_subscription_product_limit_key zone=lk_subscription_price_preview_by_ip:1m rate=60r/m;
